components:
  schemas:
    LetraComprobante:
      type: object
      properties:
        ad_client_id:
          type: integer
        ad_componentobjectuid:
          type: string
        ad_org_id:
          type: integer
        c_letra_comprobante_id:
          type: integer
        created:
          type: string
        createdby:
          type: integer
        description:
          type: string
        isactive:
          type: boolean
        isdefault:
          type: boolean
        letra:
          type: string
        updated:
          type: string
        updatedby:
          type: integer
        additionalvalues:
          type: array
          items:
            $ref: './propertiesmap.yaml#/propertiesmap'
        referencedvalues:
          type: array
          items:
            $ref: './propertiesmap.yaml#/propertiesmap'
      required:
         - ad_client_id
         - ad_org_id
         - c_letra_comprobante_id
         - created
         - createdby
         - isactive
         - isdefault
         - letra
         - updated
         - updatedby
