components:
  schemas:
    NumeroComercio:
      type: object
      properties:
        ad_client_id:
          type: integer
          description: "Compañía o empresa que utiliza ésta instalación"
        ad_org_id:
          type: integer
          description: "Entidad organizacional dentro de la compañía"
        adquirente:
          type: string
          description: " "
        c_bankaccount_id:
          type: integer
          description: "Cuenta Bancaria"
        c_bankaccount_settlement_id:
          type: integer
          description: "Cuenta Bancaria utilizada para el pago resultante en Liquidaciones"
        c_bpartner_id:
          type: integer
          description: "Identifica una Entidad Comercial"
        created:
          type: string
          description: "Fecha de creación de este registro"
        createdby:
          type: integer
          description: "Usuario que creó este registro"
        financingservice:
          type: string
          description: " "
        isactive:
          type: boolean
          description: "El registro está activo en el sistema"
        m_numerocomercio_id:
          type: integer
          description: " "
        numerocomercio:
          type: string
          description: " "
        updated:
          type: string
          description: "Determina si el campo esta actualizado"
        updatedby:
          type: integer
          description: "Determina quien actualizó el campo"
        value:
          type: string
          description: "Clave de búsqueda para el registro en el formato requerido; debe ser única"
        additionalvalues:
          type: array
          items:
            $ref: './propertiesmap.yaml#/propertiesmap'
        referencedvalues:
          type: array
          items:
            $ref: './propertiesmap.yaml#/propertiesmap'
      required:
         - ad_client_id
         - ad_org_id
         - adquirente
         - c_bankaccount_id
         - c_bpartner_id
         - created
         - createdby
         - isactive
         - m_numerocomercio_id
         - numerocomercio
         - updated
         - updatedby
