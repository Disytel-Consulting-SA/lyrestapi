components:
  schemas:
    ValRule:
      type: object
      properties:
        ad_client_id:
          type: integer
          description: "Compañía o empresa que utiliza ésta instalación"
        ad_componentobjectuid:
          type: string
          description: " "
        ad_componentversion_id:
          type: integer
          description: "Versión de Componente propietaria de este registro"
        ad_org_id:
          type: integer
          description: "Entidad organizacional dentro de la compañía"
        ad_val_rule_id:
          type: integer
          description: "Regla de validación"
        code:
          type: string
          description: "Código de Validación"
        created:
          type: string
          description: "Fecha de creación de este registro"
        createdby:
          type: integer
          description: "Usuario que creó este registro"
        description:
          type: string
          description: "Descripción corta opcional del registro"
        entitytype:
          type: string
          description: "Tipo de Entidad Diccionario; determina propiedad y sincronización"
        isactive:
          type: boolean
          description: "El registro está activo en el sistema"
        name:
          type: string
          description: "Identificador alfanumérico de la Entidad"
        type:
          type: string
          description: "Tipo de validación (SQL; Java Script; Java Language)"
        updated:
          type: string
          description: "Determina si el campo esta actualizado"
        updatedby:
          type: integer
          description: "Determina quien actualizó el campo"
        usetype:
          type: string
          description: "Indica el ámbito de utilización de una validación"
        additionalvalues:
          type: array
          items:
            $ref: './propertiesmap.yaml#/propertiesmap'
        referencedvalues:
          type: array
          items:
            $ref: './propertiesmap.yaml#/propertiesmap'
      required:
         - ad_client_id
         - ad_org_id
         - ad_val_rule_id
         - created
         - createdby
         - entitytype
         - isactive
         - name
         - type
         - updated
         - updatedby
         - usetype
