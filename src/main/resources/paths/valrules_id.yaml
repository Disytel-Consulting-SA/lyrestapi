get:
  summary: Recupera una regla de validacion en particular
  tags:
    - "valrule"
  parameters:
    - name: id
      in: path
      description: ID de la regla de validacion
      required: true
      schema:
        type: integer
  description: Recupera la informacion de una regla de validacion en particular
  operationId: retrieveValRule
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: '../model/valrule.yaml#/components/schemas/ValRule'