get:
  summary: Recupera un plan de financiacion de entidad financiera en particular
  tags:
    - "entidadfinancieraplan"
  parameters:
    - name: id
      in: path
      description: ID del plan de financiacion de entidad financiera
      required: true
      schema:
        type: integer
  description: Recupera la informacion de un plan de financiacion de entidad financiera
  operationId: retrieveEntidadFinancieraPlan
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: '../model/entidadfinancieraplan.yaml#/components/schemas/EntidadFinancieraPlan'
delete:
  summary: Elimina plan de financiacion de entidad financiera
  tags:
    - "entidadfinancieraplan"
  parameters:
    - name: id
      in: path
      description: ID del plan de financiacion de entidad financiera
      required: true
      schema:
        type: integer
  description: Elimina un plan de financiacion de entidad financiera
  operationId: deleteEntidadFinancieraPlan
  responses:
    "409":
      description: Imposible eliminar
      content:
        text/plain:
          schema:
            type: string
    "404":
      description: No encontrado
      content:
        text/plain:
          schema:
            type: string
    "204":
      description: OK
      content:
        text/plain:
          schema:
            type: string
put:
  summary: Actualiza un plan de financiacion de entidad financiera
  tags:
    - "entidadfinancieraplan"
  operationId: updateEntidadFinancieraPlan
  parameters:
    - name: id
      in: path
      description: ID del plan de financiacion de entidad financiera
      required: true
      schema:
        type: integer
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          $ref: '../model/entidadfinancieraplan.yaml#/components/schemas/EntidadFinancieraPlan'
  responses:
    "200":
      description: OK
      content:
        text/plain:
          schema:
            type: string
    "409":
      description: Imposible actualizar
      content:
        text/plain:
          schema:
            type: string
    "404":
      description: No encontrado
      content:
        text/plain:
          schema:
            type: string