get:
  summary: Recupera un tipo de retencion en particular
  tags:
    - "retenciontype"
  parameters:
    - name: id
      in: path
      description: ID del registro
      required: true
      schema:
        type: integer
  description: Recupera la informacion de un tipo de retencion en particular
  operationId: retrieveRetencionType
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: '../model/retenciontype.yaml#/components/schemas/RetencionType'
    "404":
      description: No encontrado
      content:
        application/json:
          schema:
            type: string
