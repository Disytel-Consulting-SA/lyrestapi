get:
  summary: Recupera un registro de esquemas contables en particular
  tags:
    - "acctschema"
  parameters:
    - name: id
      in: path
      description: ID del registro
      required: true
      schema:
        type: integer
  description: Recupera la informacion de un registro de esquemas contables en particular
  operationId: retrieveAcctSchemas
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: '../model/acctschema.yaml#/components/schemas/AcctSchema'
