get:
  summary: Recupera un registro de periodos contables en particular
  tags:
    - "period"
  parameters:
    - name: id
      in: path
      description: ID del registro
      required: true
      schema:
        type: integer
  description: Recupera la informacion de un registro de periodos contables en particular
  operationId: retrievePeriods
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: '../model/period.yaml#/components/schemas/Period'
