get:
  summary: Recupera una referencia en particular
  tags:
    - "reference"
  parameters:
    - name: id
      in: path
      description: ID de la referencia
      required: true
      schema:
        type: integer
  description: Recupera la informacion de una referencia en particular
  operationId: retrieveReference
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: '../model/reference.yaml#/components/schemas/Reference'