get:
  summary: Recupera un esquema de retencion en particular
  tags:
    - "retencionschema"
  parameters:
    - name: id
      in: path
      description: ID del registro
      required: true
      schema:
        type: integer
  description: Recupera la informacion de un esquema de retencion en particular
  operationId: retrieveRetencionSchema
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: '../model/retencionschema.yaml#/components/schemas/RetencionSchema'
    "404":
      description: No encontrado
      content:
        application/json:
          schema:
            type: string
