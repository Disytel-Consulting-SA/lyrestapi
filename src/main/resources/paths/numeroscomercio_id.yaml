get:
  summary: Recupera un numero de comercio en particular
  tags:
    - "numerocomercio"
  parameters:
    - name: id
      in: path
      description: ID del registro
      required: true
      schema:
        type: integer
  description: Recupera la informacion de un numero de comercio en particular
  operationId: retrieveNumeroComercio
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: '../model/numerocomercio.yaml#/components/schemas/NumeroComercio'
