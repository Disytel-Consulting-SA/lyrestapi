get:
  summary: Recupera un corte del esquema de descuento en particular
  tags:
    - "discountschemabreak"
  parameters:
    - name: id
      in: path
      description: ID del corte del esquema de descuento
      required: true
      schema:
        type: integer
  description: Recupera la informacion de un corte del esquema de descuento en particular
  operationId: retrieveDiscountSchemaBreak
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: '../model/discountschemabreak.yaml#/components/schemas/DiscountSchemaBreak'
delete:
  summary: Elimina un corte del esquema de descuento
  tags:
    - "discountschemabreak"
  parameters:
    - name: id
      in: path
      description: ID del corte del esquema de descuento
      required: true
      schema:
        type: integer
  description: Elimina un corte del esquema de descuento existente
  operationId: deleteDiscountSchemaBreak
  responses:
    "409":
      description: Imposible eliminar
      content:
        text/plain:
          schema:
            type: string
    "404":
      description: No encontrado
      content:
        text/plain:
          schema:
            type: string
    "204":
      description: OK
      content:
        text/plain:
          schema:
            type: string
put:
  summary: Actualiza un corte del esquema de descuento existente
  tags:
    - "discountschemabreak"
  operationId: updateDiscountSchemaBreak
  parameters:
    - name: id
      in: path
      description: ID del corte del esquema de descuento a actualizar
      required: true
      schema:
        type: integer
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          $ref: '../model/discountschemabreak.yaml#/components/schemas/DiscountSchemaBreak'
  responses:
    "200":
      description: OK
      content:
        text/plain:
          schema:
            type: string
    "409":
      description: Imposible actualizar
      content:
        text/plain:
          schema:
            type: string
    "404":
      description: No encontrado
      content:
        text/plain:
          schema:
            type: string