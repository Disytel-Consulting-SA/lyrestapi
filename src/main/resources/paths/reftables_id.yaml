get:
  summary: Recupera una referencia de tipo tabla en particular
  tags:
    - "reftable"
  parameters:
    - name: id
      in: path
      description: ID de la referencia de tipo tabla
      required: true
      schema:
        type: integer
  description: Recupera la informacion de una referencia de tipo tabla en particular
  operationId: retrieveRefTable
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: '../model/reftable.yaml#/components/schemas/RefTable'