get:
  summary: Recupera una referencia de tipo lista en particular
  tags:
    - "reflist"
  parameters:
    - name: id
      in: path
      description: ID de la referencia de tipo lista
      required: true
      schema:
        type: integer
  description: Recupera la informacion de una referencia de tipo lista en particular
  operationId: retrieveRefList
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: '../model/reflist.yaml#/components/schemas/RefList'