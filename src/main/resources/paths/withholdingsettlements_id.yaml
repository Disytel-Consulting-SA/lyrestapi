get:
  summary: Recupera una fila de retenciones sufridas de la liquidacion en particular
  tags:
    - "withholdingsettlement"
  parameters:
    - name: id
      in: path
      description: ID del registro
      required: true
      schema:
        type: integer
  description: Recupera la informacion de una fila de retenciones sufridas de la liquidacion en particular
  operationId: retrieveWithholdingSettlement
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: '../model/withholdingsettlement.yaml#/components/schemas/WithholdingSettlement'
    "404":
      description: No encontrado
      content:
        application/json:
          schema:
            type: string
put:
  summary: Actualiza una fila de retenciones sufridas de la liquidacion
  tags:
    - "withholdingsettlement"
  description: >
    Actualiza una fila ya existente. El uso principal es cargarle el importe, que la fila trae en cero desde
    que el modelo la genero:


        PUT /v1.0/withholdingsettlements/{id}    con body {"amount": 1234.56}


    Al guardar, el modelo recalcula solo el total Withholding de la cabecera de la liquidacion. Ese recalculo lo
    hace la clase del core, no esta API: si el total de la cabecera no cambia despues de un PUT exitoso, el
    problema esta en el core cargado, no aca.


    No se expone POST: las filas ya vienen creadas. El unico caso que quedaria sin cubrir es un concepto dado
    de alta en el maestro despues de creada la liquidacion.
  operationId: updateWithholdingSettlement
  parameters:
    - name: id
      in: path
      description: ID del registro a actualizar
      required: true
      schema:
        type: integer
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          $ref: '../model/withholdingsettlement.yaml#/components/schemas/WithholdingSettlement'
  responses:
    "200":
      description: OK
      content:
        text/plain:
          schema:
            type: string
    "409":
      description: Imposible actualizar
      content:
        text/plain:
          schema:
            type: string
    "404":
      description: No encontrado
      content:
        text/plain:
          schema:
            type: string
